<?php
 //if($_GET['id']) {
 //Atrapa con el get el id del usuario seleccionado para eliminat y tambien el tipo de deporte que practica
   /* $id_v =(int) $_GET['id'];
    $tipo =(int) $_GET['tipo'];*/
    require_once('consultas.php');
//se llama a la funcion de borrar,mandado como parametros el id y el tipo de jugador
    $monto=0;
    $fecha= date("Y-m-d");
    
    if (add_venta($monto,$fecha)) {
    	$id=id();
    	header("location: new_venta.php?id_v=".$id.""); 
    }else{
    	header("location: ventas.php");
    }
     
    function id()
    {
    	//include 'consultas.php';
    	$total_users=contar(2);
        $user=mostrar(2);
        if($total_users){ 
        	for($i=0;$i<$total_users;$i++){ 
        		$id=$user[$i]['id'];
        	}

    	}
    	return $id;
    }
                    

?>



















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































 