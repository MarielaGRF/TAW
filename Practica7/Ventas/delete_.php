<?php
 if($_GET['id']) {
 //Atrapa con el get el id del usuario seleccionado para eliminat y tambien el tipo de deporte que practica
    $id =(int) $_GET['id'];
    $tipo =(int) $_GET['tipo'];
    $id_v =(int) $_GET['id_v'];
    $cantidad=(int) $_GET['cantidad'];
    $producto=(int) $_GET['producto'];
   require_once('consultas.php');
//se llama a la funcion de borrar,mandado como parametros el id y el tipo de jugador

    borrar($id,$tipo);
    if ($tipo==1) {
    	//borrar($id,$tipo);
    	header("location: productos.php"); 
    }else{
    	//borrar($producto,$tipo);
    	$total=contar(1);
    	if (total) {
    		$datos=monto($producto);
    		$pza=$datos[0]['cantidad'];
    		$can_new=$pza+$cantidad;
    		update_cantidad($producto,$can_new);
    	header("location: detalles_venta.php?id_v=".$id_v."&tipo=".$tipo."");
    	}
    	
    }
     

 }

?>



















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































 